# Smart Task Planner

Умный планировщик задач с интеграцией AI для автоматической категоризации и оценки времени выполнения.

[![Python](https://img.shields.io/badge/Python-3.12+-blue.svg)](https://python.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)](https://fastapi.tiangolo.com)
[![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-2.0+-red.svg)](https://sqlalchemy.org)

---

## 📋 Содержание

- [О проекте](#о-проекте)
- [Технологии](#технологии)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Запуск](#запуск)
- [API Эндпоинты](#api-эндпоинты)
- [Примеры использования](#примеры-использования)
- [AI Функции](#ai-функции)
- [Тестирование](#тестирование)
- [Структура проекта](#структура-проекта)

---

## 🎯 О проекте

**Smart Task Planner** — бэкенд-приложение для управления задачами с интеграцией искусственного интеллекта.

### Возможности

| Функция | Описание |
|---------|----------|
| **Пользователи** | Регистрация, JWT-аутентификация, профиль |
| **Задачи** | CRUD операции, фильтрация по статусу и категории |
| **AI-категоризация** | Работа, личное, здоровье, обучение, другое |
| **Оценка времени** | Автоматический расчет минут на задачу |
| **Graceful degradation** | Заглушки при недоступности AI API |

---

## 🛠 Технологии

| Компонент | Технология |
|-----------|------------|
| **Backend** | FastAPI, SQLAlchemy 2.0, Pydantic |
| **База данных** | SQLite |
| **Аутентификация** | JWT (python-jose), bcrypt |
| **AI Интеграция** | OpenAI API / DeepSeek, HTTPX |
| **Тестирование** | pytest, pytest-asyncio, pytest-cov |

---

## 🚀 Установка

### Требования
- Python 3.12 или выше
- (Опционально) API ключ OpenAI или DeepSeek

### Пошаговая инструкция

```bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/smart-task-planner.git
cd smart-task-planner

# 2. Создать виртуальное окружение
python -m venv .venv

# 3. Активировать окружение
# Для Windows:
.venv\Scripts\activate
# Для Linux/Mac:
source .venv/bin/activate

# 4. Установить зависимости
pip install -r requirements.txt

# 5. Создать файл .env из примера
cp .env.example .env
```

---

## ⚙️ Конфигурация

Отредактируйте файл `.env`:

```env
# База данных
DATABASE_URL=sqlite:///./smart_planner.db

# JWT настройки
SECRET_KEY=your-super-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000"]

# Режим отладки
DEBUG=True

# AI API (опционально)
AI_API_KEY=your-api-key-here
AI_API_URL=https://api.openai.com/v1/chat/completions
AI_MODEL=gpt-3.5-turbo
```

---

## 🏃 Запуск

```bash
# Запустить сервер разработки
uvicorn app.main:app --reload
```

После запуска откройте в браузере:

| URL | Описание |
|-----|----------|
| http://localhost:8000 | Главная страница |
| http://localhost:8000/docs | Swagger UI документация |
| http://localhost:8000/redoc | ReDoc документация |
| http://localhost:8000/health | Проверка состояния |

---

## 📚 API Эндпоинты

### Аутентификация (`/auth`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `POST` | `/auth/register` | Регистрация пользователя |
| `POST` | `/auth/login` | Вход (получение токена) |
| `GET` | `/auth/me` | Информация о текущем пользователе |

### Задачи (`/tasks`)

| Метод | Эндпоинт | Описание | Параметры |
|-------|----------|----------|-----------|
| `GET` | `/tasks/` | Список задач | `skip`, `limit`, `status`, `category` |
| `GET` | `/tasks/{id}` | Задача по ID | — |
| `POST` | `/tasks/` | Создать задачу | — |
| `PUT` | `/tasks/{id}` | Обновить задачу | — |
| `DELETE` | `/tasks/{id}` | Удалить задачу | — |

### AI-функции (`/ai`)

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `POST` | `/ai/categorize` | Определить категорию задачи |
| `POST` | `/ai/estimate-time` | Оценить время выполнения |
| `POST` | `/ai/analyze` | Комплексный анализ задачи |

---

## 💡 Примеры использования

### 1. Регистрация пользователя

**Запрос:**
```bash
curl -X POST "http://localhost:8000/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "username": "john",
    "password": "secret123",
    "full_name": "John Doe"
  }'
```

**Ответ:**
```json
{
  "email": "user@example.com",
  "username": "john",
  "full_name": "John Doe",
  "id": 1,
  "created_at": "2026-02-24T10:30:00",
  "is_active": true
}
```

### 2. Получение токена

**Запрос:**
```bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=john&password=secret123"
```

**Ответ:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

### 3. Создание задачи

**Запрос:**
```bash
curl -X POST "http://localhost:8000/tasks/" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Подготовить отчет",
    "description": "Написать годовой отчет по проекту",
    "priority": "высокий",
    "deadline": "2026-03-01T18:00:00"
  }'
```

**Ответ:**
```json
{
  "id": 1,
  "title": "Подготовить отчет",
  "description": "Написать годовой отчет по проекту",
  "status": "новая",
  "priority": "высокий",
  "category": "работа",
  "estimated_time": 120,
  "deadline": "2026-03-01T18:00:00",
  "created_at": "2026-02-24T10:35:00",
  "updated_at": "2026-02-24T10:35:00",
  "owner_id": 1
}
```

### 4. Получение списка задач

**Запрос:**
```bash
curl -X GET "http://localhost:8000/tasks/?status=новая&category=работа" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Ответ:**
```json
[
  {
    "id": 1,
    "title": "Подготовить отчет",
    "status": "новая",
    "category": "работа",
    "priority": "высокий"
  }
]
```

### 5. AI-анализ задачи

**Запрос:**
```bash
curl -X POST "http://localhost:8000/ai/analyze" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "task_description": "Подготовить презентацию для встречи с клиентом"
  }'
```

**Ответ:**
```json
{
  "category": "работа",
  "estimated_time": 60,
  "subtasks": [
    "Исследовать требования",
    "Составить структуру",
    "Подготовить слайды"
  ],
  "suggested_priority": "средний",
  "tips": [
    "⏱️ Примерное время: 60 минут",
    "📂 Категория: работа",
    "🎯 Начните с исследования"
  ]
}
```

---

## 🤖 AI Функции

### Режимы работы

| Режим | Условие | Поведение |
|-------|---------|-----------|
| **Реальный AI** | Есть API ключ | Запросы к OpenAI/DeepSeek |
| **Заглушки** | Нет API ключа | Категоризация по ключевым словам |

### Категоризация (режим заглушек)

| Категория | Ключевые слова |
|-----------|----------------|
| `работа` | работ, проект, клиент, отчет, презентац |
| `личное` | личн, семь, друз, хобби, отдых |
| `здоровье` | спорт, трен, здоров, врач, больниц |
| `обучение` | учит, курс, книг, лекц, образов |
| `другое` | всё остальное |

### Оценка времени (режим заглушек)

| Условие | Время |
|---------|-------|
| В описании есть "минут" | 15 мин |
| В описании есть "час" | 60 мин |
| Есть слова "срочн", "быстр" | 10 мин |
| Есть слова "больш", "сложн" | 120 мин |
| По умолчанию | 30 мин |

---

## 🧪 Тестирование

### Установка зависимостей для тестов

```bash
pip install pytest pytest-asyncio pytest-cov httpx
```

### Запуск тестов

```bash
# Все тесты
pytest

# С подробным выводом
pytest -v

# Конкретный файл
pytest tests/test_ai_parsing.py -v

# С покрытием кода
pytest --cov=app tests/

# HTML отчет о покрытии
pytest --cov=app --cov-report=html tests/
```

### Структура тестов

```
tests/
├── test_ai_parsing.py    # Тесты AI-функций
└── test_tasks.py         # Тесты CRUD операций
```

### Пример теста

```python
import pytest
from app.services.ai_service import AIService
from app.schemas import TaskCategoryEnum

@pytest.mark.asyncio
async def test_categorize_task_work_keywords():
    service = AIService()
    service.use_mock = True
    
    description = "Подготовить отчет для клиента"
    category = await service.categorize_task(description)
    
    assert category == TaskCategoryEnum.WORK
```

---

## 📁 Структура проекта

```
smart-task-planner/
├── app/
│   ├── core/                    # Конфигурация и безопасность
│   │   ├── config.py
│   │   └── security.py
│   ├── routers/                  # API эндпоинты
│   │   ├── auth.py
│   │   ├── tasks.py
│   │   └── ai.py
│   ├── services/                  # Бизнес-логика
│   │   └── ai_service.py
│   ├── __init__.py
│   ├── database.py                # Подключение к БД
│   ├── main.py                     # Точка входа
│   └── schemas.py                  # Модели данных
├── tests/                          # Тесты
│   ├── __init__.py
│   ├── test_ai_parsing.py
│   └── test_tasks.py
├── .env.example                    # Пример переменных
├── pytest.ini                       # Конфигурация pytest
├── requirements.txt                 # Зависимости
├── AI_ASSISTANT_LOG.md               # Лог взаимодействия с AI
└── README.md                         # Документация
```

---

## 👥 Авторы

[@yushmeh](https://github.com/yushmeh)
