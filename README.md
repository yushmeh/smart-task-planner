**Smart Task Planner
Умный планировщик задач с интеграцией AI для автоматической категоризации и оценки времени выполнения.

https://img.shields.io/badge/Python-3.12+-blue.svg
https://img.shields.io/badge/FastAPI-0.104+-green.svg
https://img.shields.io/badge/SQLAlchemy-2.0+-red.svg
https://img.shields.io/badge/License-MIT-yellow.svg

📋 Содержание
О проекте

Технологический стек

Архитектура

Установка и запуск

API Эндпоинты

Примеры использования

AI Функции

Тестирование

AI Assistant Log

Roadmap

Лицензия

🎯 О проекте
Smart Task Planner — это бэкенд-приложение для управления задачами с интеграцией искусственного интеллекта. Проект демонстрирует современный подход к разработке API с использованием FastAPI и лучших практик.

Основные возможности
✅ Управление пользователями: регистрация, JWT-аутентификация, профиль

✅ Управление задачами: CRUD операции с фильтрацией по статусу и категории

✅ AI-интеграция:

Автоматическая категоризация задач (работа, личное, здоровье, обучение, другое)

Оценка времени выполнения

Разбиение сложных задач на подзадачи

✅ Локальный AI: поддержка Ollama для приватной обработки данных

✅ Graceful degradation: работа в режиме заглушек при недоступности AI API

✅ Документация: интерактивная Swagger UI

🛠 Технологический стек
Backend
FastAPI — современный веб-фреймворк

SQLAlchemy 2.0 — ORM для работы с БД

Pydantic — валидация данных

SQLite — база данных (разработка)

Аутентификация
JWT (python-jose) — токены доступа

bcrypt — хеширование паролей

OAuth2 — стандарт аутентификации

AI Интеграция
OpenAI API / DeepSeek — облачные модели

HTTPX — асинхронные HTTP запросы

Tenacity — повторные попытки с экспоненциальной задержкой

Тестирование
pytest — unit-тесты

pytest-asyncio — тестирование асинхронного кода

pytest-cov — покрытие кода

TestClient — тестирование API

🏗 Архитектура
text
smart-task-planner/
├── app/
│   ├── core/                    # Ядро приложения
│   │   ├── config.py            # Конфигурация (переменные окружения)
│   │   └── security.py          # JWT, хеширование паролей
│   ├── routers/                  # API эндпоинты
│   │   ├── auth.py               # Аутентификация (/auth/*)
│   │   ├── tasks.py              # Задачи (/tasks/*)
│   │   └── ai.py                 # AI-функции (/ai/*)
│   ├── services/                  # Бизнес-логика
│   │   └── ai_service.py          # AI сервис (заглушки, OpenAI)
│   ├── __init__.py
│   ├── database.py                # Подключение к БД
│   ├── main.py                     # Точка входа
│   └── schemas.py                  # Pydantic + SQLAlchemy модели
├── tests/                          # Тесты
│   ├── __init__.py
│   ├── test_ai_parsing.py
│   └── test_tasks.py
├── .env.example                    # Пример переменных окружения
├── pytest.ini                       # Конфигурация pytest
├── requirements.txt                 # Зависимости
├── AI_ASSISTANT_LOG.md               # Лог взаимодействия с AI
└── README.md                         # Документация
🚀 Установка и запуск
Требования
Python 3.12+

Быстрый старт
bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/smart-task-planner.git
cd smart-task-planner

# 2. Создать виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
.venv\Scripts\activate     # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Настроить переменные окружения
cp .env.example .env
# Отредактируйте .env (добавьте SECRET_KEY)

# 5. Запустить сервер
uvicorn app.main:app --reload

# 6. Открыть документацию
# http://localhost:8000/docs
Настройка переменных окружения
Создайте файл .env на основе .env.example:

env
# База данных
DATABASE_URL=sqlite:///./smart_planner.db

# JWT настройки
SECRET_KEY=your-super-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000"]

# Настройки приложения
DEBUG=True

# AI API (опционально - без ключа работают заглушки)
AI_API_KEY=your-api-key-here
AI_API_URL=https://api.openai.com/v1/chat/completions
AI_MODEL=gpt-3.5-turbo
📚 API Эндпоинты
Аутентификация (/auth)
Метод	Эндпоинт	Описание	Тело запроса
POST	/auth/register	Регистрация пользователя	{email, username, password, full_name}
POST	/auth/login	Вход (получение токена)	{username, password} (form-data)
GET	/auth/me	Информация о текущем пользователе	-
Задачи (/tasks)
Метод	Эндпоинт	Описание	Параметры
GET	/tasks/	Список задач	skip, limit, status, category
GET	/tasks/{id}	Получить задачу по ID	-
POST	/tasks/	Создать задачу	{title, description, priority, deadline}
PUT	/tasks/{id}	Обновить задачу	{title, status, priority, ...}
DELETE	/tasks/{id}	Удалить задачу	-
AI-функции (/ai)
Метод	Эндпоинт	Описание
POST	/ai/categorize	Определить категорию задачи
POST	/ai/estimate-time	Оценить время выполнения
POST	/ai/analyze	Комплексный анализ задачи
💡 Примеры использования
Регистрация пользователя
bash
curl -X POST "http://localhost:8000/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "username": "john",
    "password": "secret123",
    "full_name": "John Doe"
  }'
Ответ:

json
{
  "email": "user@example.com",
  "username": "john",
  "full_name": "John Doe",
  "id": 1,
  "created_at": "2026-02-24T10:00:00",
  "is_active": true
}
Получение токена
bash
curl -X POST "http://localhost:8000/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=john&password=secret123"
Ответ:

json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
Создание задачи
bash
curl -X POST "http://localhost:8000/tasks/" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Подготовить отчет",
    "description": "Написать годовой отчет по проекту",
    "priority": "высокий",
    "deadline": "2026-03-01T18:00:00"
  }'
AI-анализ задачи
bash
curl -X POST "http://localhost:8000/ai/analyze" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "task_description": "Подготовить презентацию для встречи с клиентом"
  }'
Ответ (с реальным AI API):

json
{
  "category": "работа",
  "estimated_time": 60,
  "subtasks": [
    "Исследовать требования",
    "Составить план",
    "Подготовить черновик",
    "Сделать ревью"
  ],
  "suggested_priority": "средний",
  "tips": [
    "⏱️ На выполнение уйдет примерно 60 минут",
    "📂 Категория: работа",
    "🎯 Начните с самого важного"
  ]
}
Ответ (режим заглушек):

json
{
  "category": "работа",
  "estimated_time": 30,
  "subtasks": [
    "Начать работу",
    "Выполнить основную часть",
    "Проверить результат"
  ],
  "suggested_priority": "средний",
  "tips": [
    "⏱️ На выполнение уйдет примерно 30 минут",
    "📂 Категория: работа",
    "🎯 Начните с самого важного"
  ]
}
🤖 AI Функции
Режимы работы
Заглушки (Mock) — автоматически включаются при отсутствии API ключа. Используют правила на основе ключевых слов.

Облачный AI — интеграция с OpenAI API (при наличии ключа).

Автокатегоризация
Категория	Ключевые слова (для заглушек)
работа	работ, проект, клиент, отчет, презентац, бизнес
личное	личн, семь, друз, хобби, отдых, развлек
здоровье	спорт, трен, здоров, врач, больниц, медиц
обучение	учит, курс, книг, лекц, образов, школ
другое	все остальное
Оценка времени
Тип задачи	Примерное время (заглушки)
Быстрые задачи (минут)	10-15 минут
Средние задачи	30-60 минут
Длительные задачи	2-4 часа
Сложные проекты	8+ часов
🧪 Тестирование
Запуск тестов
bash
# Все тесты
pytest

# С подробным выводом
pytest -v

# Конкретный файл
pytest tests/test_ai_parsing.py -v

# С покрытием кода
pytest --cov=app tests/

# HTML отчет о покрытии
pytest --cov=app --cov-report=html tests/
Структура тестов
text
tests/
├── test_ai_parsing.py     # Тесты AI-функций (категоризация, оценка времени)
└── test_tasks.py          # Тесты CRUD операций с задачами
Пример теста
python
@pytest.mark.asyncio
async def test_categorize_task_work_keywords(ai_service):
    description = "Подготовить отчет для клиента"
    category = await ai_service.categorize_task(description)
    assert category == TaskCategoryEnum.WORK
📝 AI Assistant Log
Подробный лог всех ключевых взаимодействий с AI Assistant в процессе разработки доступен в файле AI_ASSISTANT_LOG.md.

Основные этапы
Проектирование архитектуры — выбор структуры проекта

Реализация аутентификации — JWT, bcrypt, обработка длинных паролей

AI-заглушки — категоризация и оценка времени на правилах

Продвинутая обработка ошибок — retry, параллельные запросы, graceful degradation

👥 Авторы
@yushmeh**
